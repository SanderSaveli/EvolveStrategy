public static class BattleConstants
{
    /// <summary>
    /// Evolution points awarded for one spawn in the nest
    /// </summary>
    public const int EVOLVE_POINTS_PER_SPAWN = 1;
    /// <summary>
    /// The time it takes for one unit to spawn on a normal cell at 100% spawn rate
    /// </summary>
    public const float DEFAULT_TIME_TO_SPAWN = 4;
    /// <summary>
    /// The time it takes for one unit to spawn on a nest cell at 100% spawn rate
    /// </summary>
    public const float DEFAULT_TIME_TO_NEST_SPAWN = 1.5f;
    /// <summary>
    /// The time it takes for a unit to die on a cell with an unfavorable climate with 0% resistance
    /// </summary>
    public const float DEFAULT_TIME_TO_DIE = 3;

    /// <summary>
    /// Time required to move to a normal tile at 100% movement speed
    /// </summary>
    public const float DEFAUL_TIME_TO_WALCK = 5;

    public const int START_UNIT_ATTACK = 10;
    public const int START_UNIT_DEFENSE = 10;
    public const float START_UNIT_WALCK_SPEED = 1;
    public const float START_UNIT_CLIMB_SPEED = 0.25f;
    public const float START_UNIT_SWIMMING_SPEED = 0.25f;
    public const float START_UNIT_SPAWN_SPEED = 1;
    public const float START_UNIT_POISON_RESISTANCE = 0.25f;
    public const float START_UNIT_HEAT_RESISTANCE = 0.25f;
    public const float START_UNIT_COLD_RESISTANCE = 0.25f;
}
